#ifndef Queue_h
#define Queue_h

// The Node data structure
struct Node {
    long data;
    Node* next;
    
    Node(long d) {
        data = d;
        next = NULL;
    }
    
    ~Node(){
        
    }
};


struct Queue {
    // Implement the Queue data structure so that the code works...
};

#endif
